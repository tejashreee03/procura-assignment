version: '3'
services:
    postgres:
      image: postgres:16
      container_name: postgres
      restart: always
      environment:
       POSTGRES_USER: root
       POSTGRES_PASSWORD: jayesh@2512
       POSTGRES_DB: procuraDB
      ports:
      - '5432:5432'
      volumes:
      - ./data:/var/lib/postgresql/data

